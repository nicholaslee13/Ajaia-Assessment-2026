import { SignInForm } from "@/components/auth/sign-in-form";

export default function HomePage() {
  return (
    <main>
      <p>A deployment-safe preview of the auth entry screen for the collaborative doc editor.</p>
      <SignInForm />
    </main>
  );
}
